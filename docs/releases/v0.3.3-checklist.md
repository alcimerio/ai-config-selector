# v0.3.3 Release Checklist

This is the completed evidence record for v0.3.3. Pre-tag authorization,
tag-triggered evidence, and public-release evidence were separate phases.
Only public, sanitized facts are recorded here; credentials, personal paths,
account data, signed URLs, generated policies, environment values, and raw
terminal output are excluded.

## Relationship to the unpublished v0.3.0, v0.3.1, and v0.3.2 candidates

The `v0.3.0`, `v0.3.1`, and `v0.3.2` tags exist remotely and are never moved,
deleted, or reused. Required native macOS gates failed, so the
immutable-publication policy withheld all three Releases. v0.3.3 corrected the
identity-revalidation race exposed by the v0.3.2 tag run. The
[v0.3.0 checklist](v0.3.0-checklist.md),
[v0.3.1 checklist](v0.3.1-checklist.md), and
[v0.3.2 checklist](v0.3.2-checklist.md) remain historical records and do not
authorize or alter any tag, Release, or asset.

## Pre-tag authorization evidence

Pre-tag authorization completed against protected-main source
`233ad4adc78fa4f326524ddd7a0cf2fde12aa2a0` before the tag was created or
pushed.

- Candidate version `v0.3.3`, version-controlled notes, support matrix, Profile
  compatibility, certified-host requirement, native-backend requirement, and
  lack of an unsandboxed fallback were reviewed together.
- Required protected-main checks passed: [CI](https://github.com/alcimerio/ai-config-selector/actions/runs/32208346940)
  on attempt 2, [macOS validation](https://github.com/alcimerio/ai-config-selector/actions/runs/32208346943),
  and [promoted-artifact validation](https://github.com/alcimerio/ai-config-selector/actions/runs/32208346946).
- `go test -count=1 ./scripts -run V033`, the complete scripts suite,
  `scripts/release-candidate.sh v0.3.3`, and `git diff --check` passed.
- The transient identity reread sent no signal without revalidation;
  persistent ambiguity remained bounded and fail-closed; and concurrent macOS
  stress passed on both architectures.
- Immutable Releases, the release environment, protected `refs/tags/v*`
  creation, non-bypassable update/deletion rules, the authorized tag creator,
  policy-App client configuration, and release-environment private-key secret
  presence were reviewed without reading or recording secret values.

### Pre-tag native-evidence contract review

Pre-tag review of [issue #62](https://github.com/alcimerio/ai-config-selector/issues/62),
the [native candidate contract](../../acceptance/promoted_artifact_native_test.go),
the [candidate lifecycle contract](../../acceptance/promoted_artifact_test.go),
and the [four-target release workflow](../../.github/workflows/release.yml)
confirmed that one candidate would be reused without native rebuilds. macOS
required verified system Seatbelt; Ubuntu required verified signed-system
Bubblewrap and the targeted AppArmor configuration.

Backend, containment, lifecycle, safe-diagnostic, and limitation coverage were
reviewed. The required limitations remained explicit: no egress firewall, no
defense against a compromised administrator, repository-local Skills remained
Devin-controlled, and the broad Seatbelt prototype remained feasibility
history. This source-and-contract review did not complete any v0.3.3 tag-run
row or claim that an archive had passed on a native runner.

## Local tag preparation and push authorization

Local candidate validation completed from clean, freshly fetched `main` whose
`HEAD` equaled `origin/main`. The annotated tag message was `Release v0.3.3`.

- Annotated tag object: `be86b8fd279d42a69f048e3aa9dfbacbb62f8061`.
- Peeled protected-main source: `233ad4adc78fa4f326524ddd7a0cf2fde12aa2a0`.
- The maintainer inspected both identities and separately authorized the exact
  command `git push origin refs/tags/v0.3.3`.

The local tag did not authorize its own push. No existing release tag was
moved, deleted, or reused.

## Tag-triggered workflow evidence

The [Immutable release workflow](https://github.com/alcimerio/ai-config-selector/actions/runs/32304406158)
completed successfully on attempt 1. Its build job produced one exact candidate
artifact set for the annotated tag.

| Required target and backend | Tag-run job and final outcome |
| --- | --- |
| macOS 26 `darwin/arm64` — verified system Seatbelt | [Native darwin/arm64](https://github.com/alcimerio/ai-config-selector/actions/runs/32304406158/job/96234035066) — passed |
| macOS 26 `darwin/amd64` — verified system Seatbelt | [Native darwin/amd64](https://github.com/alcimerio/ai-config-selector/actions/runs/32304406158/job/96234035044) — passed |
| Ubuntu 24.04 LTS `linux/amd64` — verified signed-system Bubblewrap and targeted AppArmor profile | [Native linux/amd64](https://github.com/alcimerio/ai-config-selector/actions/runs/32304406158/job/96234035053) — passed |
| Ubuntu 24.04 LTS `linux/arm64` — verified signed-system Bubblewrap and targeted AppArmor profile | [Native linux/arm64](https://github.com/alcimerio/ai-config-selector/actions/runs/32304406158/job/96234035113) — passed |

Every native job consumed the supplied bytes without rebuilding and emitted
only sanitized observations. [Exact-byte attestation](https://github.com/alcimerio/ai-config-selector/actions/runs/32304406158/job/96234740577)
and the immutable-publication policy passed. The
[publication job](https://github.com/alcimerio/ai-config-selector/actions/runs/32304406158/job/96234839199)
also passed.

The optional authenticated smoke and pre-tag review did not replace these
v0.3.3 tag-run outcomes.

## Public-release evidence

The public [ACS v0.3.3 Release](https://github.com/alcimerio/ai-config-selector/releases/tag/v0.3.3)
was published at `2026-08-19T21:36:37Z`. It is immutable, is neither a draft nor
a prerelease, and has the following identity:

- Release database identifier: `373365098`.
- Tag ref: `refs/tags/v0.3.3`.
- Annotated tag object: `be86b8fd279d42a69f048e3aa9dfbacbb62f8061`.
- Peeled source and Release target:
  `233ad4adc78fa4f326524ddd7a0cf2fde12aa2a0`.

| Public asset | Size in bytes | SHA-256 |
| --- | ---: | --- |
| `acs_0.3.3_darwin_amd64.tar.gz` | 1,889,195 | `76d073bfda05fc9dccaf86cbe1064a60504f3b88d8ed401a062306a4440eb1c4` |
| `acs_0.3.3_darwin_arm64.tar.gz` | 1,799,488 | `bccef53cbc6175bf28fc6a9e913208d03ebec401edb90a306864e57e7668ee21` |
| `acs_0.3.3_linux_amd64.tar.gz` | 1,796,805 | `165b4ec8e3bc74a67a769539e68d0e3b335ce9b088465e4553f45e91b091e929` |
| `acs_0.3.3_linux_arm64.tar.gz` | 1,649,823 | `dc8effa6a408cfd10c6c4bb31a44b278ead3168d584af88eebe907709d2ff8ac` |
| `SHA256SUMS` | 382 | `7fc4381e15c7b7ea2487af9414ca11cbf5a9a9afec37c9404152e56ae7fb03de` |
| `install.sh` | 8,458 | `01e0fc494083e980eb3fa0c1715caaf0d6aaaf7c3f17513ccf26e61859d055f1` |

The four downloaded archives passed the public `SHA256SUMS` manifest. GitHub
build-provenance verification passed for those four archives and
`SHA256SUMS`; each subject identifies `refs/tags/v0.3.3`, source
`233ad4adc78fa4f326524ddd7a0cf2fde12aa2a0`, and the repository release
workflow. The downloaded public assets therefore match the workflow candidate
and provenance subjects.

v0.3.3 is the first published process-isolation release and supersedes the
unpublished v0.3.0, v0.3.1, and v0.3.2 candidates.

## Auditable post-publication follow-up

This record was prepared on `docs/v0.3.3-publication-evidence`, created from a
freshly fetched protected `main`, for the documentation-only PR
`docs(release): record v0.3.3 publication evidence`. It records the
already-published result and does not authorize or alter the immutable tag,
Release, or assets. It contains only sanitized public workflow and job URLs,
target outcomes, public asset hashes, provenance results, and immutable Release
identity.
