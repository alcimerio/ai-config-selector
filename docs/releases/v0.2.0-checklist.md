# v0.2.0 Release Checklist

This checklist records the small set of decisions and public identities needed
to publish v0.2.0. `INCOMPLETE` never means success. Do not record credentials,
personal paths, account details, signed URLs, or raw terminal output.

## Source and repository controls

- Candidate version: `v0.2.0`
- Exact protected-main source commit: **INCOMPLETE**
- Required pull-request and protected-main checks: **INCOMPLETE**
- Version-controlled release notes reviewed: **INCOMPLETE**
- Immutable Releases enabled: **VERIFIED 2026-08-09**
- `release` environment restricted to `v*` tags, with the policy App secret:
  **VERIFIED 2026-08-09**
- Active `refs/tags/v*` creation, update, and deletion rules verified:
  **VERIFIED 2026-08-09**
- Authorized tag creator and policy App variables verified:
  **VERIFIED 2026-08-09**

## Optional risk-triggered smoke

The real-Devin smoke is not required for every release. Run it on the
maintainer's macOS 26 darwin/arm64 host before the first release, or when the
change affects authentication, the Devin Adapter, Profile selection, Session
isolation, or interactive lifecycle behavior.

- Smoke required by this release's risk: **YES — first release**
- macOS 26 darwin/arm64 outcome and cleanup: **PASSED 2026-08-09**

No Ubuntu credentialed host is required. Ubuntu 24.04 LTS is covered by the
native credential-free release jobs below.

## Tag authorization

From a clean, freshly fetched `main` that exactly matches `origin/main`:

```sh
git fetch origin main
scripts/prepare-release-tag.sh v0.2.0
```

- Local preparation completed without pushing: **INCOMPLETE**
- Annotated tag name and message: `v0.2.0`, `Release v0.2.0`
- Tag-object SHA and peeled protected-main source commit: **INCOMPLETE**
- Maintainer inspected the identities and authorized the tag push: **INCOMPLETE**
- Exact tag pushed with `git push origin refs/tags/v0.2.0`: **INCOMPLETE**

The tag push is the sole human release authorization. GitHub Actions owns all
candidate, validation, attestation, staging, and publication transitions.

## Required release workflow

The workflow builds the candidate once and transfers those exact bytes to all
four native jobs. Every job must pass:

| Required native target | Run URL and result |
| --- | --- |
| macOS 26 `darwin/arm64` | **INCOMPLETE** |
| macOS 26 `darwin/amd64` | **INCOMPLETE** |
| Ubuntu 24.04 LTS `linux/amd64` | **INCOMPLETE** |
| Ubuntu 24.04 LTS `linux/arm64` | **INCOMPLETE** |

- Candidate build job: **INCOMPLETE**
- Exact-byte attestation job: **INCOMPLETE**
- Policy verification and immutable publication job: **INCOMPLETE**
- Release workflow URL and final result: **INCOMPLETE**
- Public Release URL: **INCOMPLETE**

## Public asset verification

- `acs_0.2.0_darwin_arm64.tar.gz`: **INCOMPLETE**
- `acs_0.2.0_darwin_amd64.tar.gz`: **INCOMPLETE**
- `acs_0.2.0_linux_amd64.tar.gz`: **INCOMPLETE**
- `acs_0.2.0_linux_arm64.tar.gz`: **INCOMPLETE**
- `SHA256SUMS`: **INCOMPLETE**
- `install.sh`: **INCOMPLETE**
- Public asset names, sizes, and SHA-256 values match the candidate:
  **INCOMPLETE**
- GitHub provenance verifies for all four archives and `SHA256SUMS`:
  **INCOMPLETE**
- Release is published and immutable at the expected tag and source commit:
  **INCOMPLETE**
