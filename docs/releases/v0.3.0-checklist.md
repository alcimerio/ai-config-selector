# v0.3.0 Release Checklist

`INCOMPLETE` never means success. Do not record credentials, personal paths,
account details, signed URLs, target output, generated policies, environment
values, or raw terminal output.

## Contract and source

- Candidate version: `v0.3.0`
- Exact protected-main source commit: **INCOMPLETE**
- Version-controlled [v0.3.0 notes](v0.3.0.md) reviewed: **INCOMPLETE**
- README, architecture, contributing guidance, notes, and checklist use the
  same supported matrix: **INCOMPLETE**
- Existing Profile files and selections remain compatible; launch requires a
  certified host and ready native backend: **INCOMPLETE**
- No public containment claim relies on a source build or optional real-Devin
  smoke: **INCOMPLETE**
- Documentation/checklist drift test (`go test ./scripts -run V030`):
  **INCOMPLETE**

## Native evidence contract

The release proof is [issue #62](https://github.com/alcimerio/ai-config-selector/issues/62),
the [native candidate contract](../../acceptance/promoted_artifact_native_test.go),
the [candidate lifecycle contract](../../acceptance/promoted_artifact_test.go),
and the [four-target workflow](../../.github/workflows/release.yml). They must
exercise the exact supplied candidate; no native job may rebuild it.

- Backend selection and missing-backend no-target case: **INCOMPLETE**
- Allowed and denied paths, including symlink escape: **INCOMPLETE**
- Environment allowlist/exclusions, terminal descriptors, and host Unix
  sockets: **INCOMPLETE**
- Outbound IP behavior recorded as a limitation, not an egress-firewall claim:
  **INCOMPLETE**
- Skill/authentication preflight, terminal signal/resize/exit, descendants,
  concurrent leases, and Session cleanup: **INCOMPLETE**
- Safe categories and suppression of private diagnostics: **INCOMPLETE**

## Ubuntu prerequisite and compatibility record

- Bubblewrap came from reviewed configured signed apt sources; no replacement
  binary was downloaded: **INCOMPLETE**
- `/usr/bin/bwrap` package ownership, matching architecture, packaged checksum,
  and user-namespace probe succeeded: **INCOMPLETE**
- Targeted `bwrap-userns-restrict` AppArmor profile for `/usr/bin/bwrap` was
  reviewed, SHA-256 verified, and loaded without disabling AppArmor or global
  user-namespace restrictions: **INCOMPLETE**
- Remaining limitations recorded: ACS is not an egress firewall, the package
  check does not defend against a compromised administrator, repository-local
  Skills remain Devin-controlled, and the historical broad Seatbelt prototype
  is feasibility history only: **INCOMPLETE**

## Required native matrix

| Required target and backend | Run URL and result |
| --- | --- |
| macOS 26 `darwin/arm64` — verified system Seatbelt | **INCOMPLETE** |
| macOS 26 `darwin/amd64` — verified system Seatbelt | **INCOMPLETE** |
| Ubuntu 24.04 LTS `linux/amd64` — verified signed-system Bubblewrap | **INCOMPLETE** |
| Ubuntu 24.04 LTS `linux/arm64` — verified signed-system Bubblewrap | **INCOMPLETE** |

Any unavailable or failing native row blocks the release. The optional
authenticated smoke is supplemental only and must not waive a native failure.

## Local candidate and tag authorization

From a clean, freshly fetched `main` matching `origin/main`:

```sh
git fetch origin main
scripts/release-candidate.sh v0.3.0
scripts/prepare-release-tag.sh v0.3.0
```

- Candidate validation completed without a tag or GitHub Release: **INCOMPLETE**
- Annotated tag name and message: `v0.3.0`, `Release v0.3.0`
- Tag-object SHA and peeled protected-main source commit: **INCOMPLETE**
- Maintainer inspected identities and authorized the tag push: **INCOMPLETE**
- Exact tag push, when authorized: `git push origin refs/tags/v0.3.0`

Do not create, move, or push the tag until every required item is complete.

## Publication and public assets

- Candidate build and four native jobs: **INCOMPLETE**
- Exact-byte attestation job: **INCOMPLETE**
- Immutable-publication policy verification: **INCOMPLETE**
- Release workflow URL and final result: **INCOMPLETE**
- Public Release URL: **INCOMPLETE**
- `acs_0.3.0_darwin_arm64.tar.gz`: **INCOMPLETE**
- `acs_0.3.0_darwin_amd64.tar.gz`: **INCOMPLETE**
- `acs_0.3.0_linux_amd64.tar.gz`: **INCOMPLETE**
- `acs_0.3.0_linux_arm64.tar.gz`: **INCOMPLETE**
- `SHA256SUMS` and `install.sh`: **INCOMPLETE**
- Public asset names, sizes, and SHA-256 values match the candidate:
  **INCOMPLETE**
- Provenance verifies for all four archives and `SHA256SUMS`: **INCOMPLETE**
- Public Release is immutable at the expected tag and source commit:
  **INCOMPLETE**
